{
  "name": "gnome-ai-usage-indicator",
  "version": "1.0.0",
  "description": "GNOME Shell extension to monitor AI coding provider usage",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@girs/gjs": "^4.0.0",
    "@girs/gnome-shell": "^48.0.0"
  }
}
